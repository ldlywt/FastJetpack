apply from:"../basic.gradle"

def netDependencies = rootProject.ext.NetworkDepend

dependencies {

    netDependencies.each { k, v -> api v }
    implementation(rootProject.ext.ThirdDepend.livedatabus)
}
